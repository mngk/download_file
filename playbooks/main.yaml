---

- hosts: all
  gather_facts: no
  tasks:
    - name: TEST
      file:
        path: /tmp/gk_test
        state: directory
        force: yes
      delegate_to: localhost
      
    - name: Debug
      shell: "curl icanhazip.com"
      delegate_to: localhost
