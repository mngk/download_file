---

- hosts: all
  gather_facts: no
  tasks:
    - name: TEST
      file:
        paths: /tmp/gk_test
        state: directory
      delegate_to: localhost
      
    - name: Debug
      shell: "curl icanhazip.com"
      delegate_to: localhost
