---

- hosts: all
  gather_facts: no
  tasks:
    - name: TEST
      file:
        path: /tmp/gk_test
        state: directory
      delegate_to: localhost
      
    - name: copy
      copy:
        src: "/var/lib/awx/test.txt"
        dest: "/tmp/test.txt"
      delegate_to: localhost
      
    - name: Debug
      shell: "curl icanhazip.com"
      delegate_to: localhost
