---

- hosts: all
  gather_facts: no
  tasks:
    - name: TEST
      file:
        path: /var/lib/awx/gk_test
        state: directory
      delegate_to: localhost
      
    - name: Debug
      shell: "curl icanhazip.com"
      delegate_to: localhost
